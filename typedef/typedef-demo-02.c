#include <stdio.h>
#include <string.h>

typedef int* i_pointer;

int main( ) {

   i_pointer p;         // int *p
   i_pointer a, *b;     // int *a, **b
   i_pointer myArr[10]; // int *myArr

   return 0;
}
